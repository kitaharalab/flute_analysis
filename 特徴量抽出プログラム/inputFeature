def inputFeature(array,data,sr):

  rms = librosa.feature.rms(y=data,hop_length = 512*4)#hop_length512に変更
  array[0].append(rangeVol(data))
  array[1].append(divAvgVol(data))

  fundData = createFundData(data)
  array[2].append(rangeFund(fundData))
  array[3].append(divAvgFund(fundData))

  #for i in range(0,4): array[i+4].append(overtoneFeature(data,sr,0)[i])
  #倍音関連の関数追加
