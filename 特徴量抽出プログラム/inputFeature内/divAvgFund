def divAvgFund(data):
  divsum = 0
  array = data
  cnt = 0

  for i in range(1,len(array)):
    if array2[i] == array[i] and array[i-1] == array[i-1] and array[i] >= 0 and array[i-1] >= 0:
      cnt += 1
      divsum += abs(array[i]-array[i-1])
    else:
      divsum += 0
  return divsum/cnt
